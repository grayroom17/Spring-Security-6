spring:

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: client-id
            client-secret: client-secret

  liquibase:
    enabled: false

  autoconfigure:
    exclude:
      org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

